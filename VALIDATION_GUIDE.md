# hierNet 検証ガイド

## テスト実行方法

```bash
cd /home/user/HierNet
Rscript test_patterns.R
```

## 各パターンの期待結果

### 1. `main_linear` - 主効果のみモデル

**真のモデル:**
```
Y = 5 + 2.0×X1 + 1.5×X2 + 0.8×X3 + 0.3×X4 + 0×X5 + ε
```

**期待される結果:**
| 指標 | Strong | Weak |
|------|--------|------|
| R² | ≥ 0.85 | ≥ 0.85 |
| 主効果選択 | X1, X2, X3 (X4は微妙) | 同左 |
| 交互作用選択 | 0 | 0 |

**検証ポイント:**
- 交互作用が0であることを確認
- X5係数が0または極めて小さい

---

### 2. `strong_interact` - 強い交互作用モデル

**真のモデル:**
```
Y = 5 + 1.5×X1 + 1.5×X2 + 0.5×X3 + 1.8×(X1×X2) + 0.8×(X2×X3) + ε
```

**期待される結果:**
| 指標 | Strong | Weak |
|------|--------|------|
| R² | ≥ 0.90 | ≥ 0.90 |
| 主効果選択 | X1, X2, X3 | X1, X2, X3 |
| 交互作用選択 | X1×X2, X2×X3 | X1×X2, X2×X3 |

**検証ポイント:**
- Strong hierarchyで両主効果と交互作用が選択される
- X1×X2の係数 > X2×X3の係数

---

### 3. `weak_interact` - 弱い階層の交互作用モデル

**真のモデル:**
```
Y = 5 + 0×X1 + 1.5×X2 + 0×X3 + 2.0×(X1×X2) + ε
```

**期待される結果:**
| 指標 | Strong | Weak |
|------|--------|------|
| R² (Strong) | 低め (0.5-0.7) | 高め (≥0.85) |
| 主効果選択 | X2のみ or X1,X2強制 | X2のみ可能 |
| 交互作用選択 | 制約により制限 | X1×X2 |

**検証ポイント:**
- **これがStrong vs Weakの違いを最も顕著に示すパターン**
- Strong: X1の主効果が0なので交互作用が選択されにくい
- Weak: X2だけあればX1×X2が選択可能

---

### 4. `quadratic` - 二次モデル

**真のモデル:**
```
Y = 10 + 0.5×X1 - 0.5×X2 + 2.0×X1² - 1.5×X2² + 1.0×(X1×X2) + ε
```

**期待される結果:**
| 指標 | Strong | Weak |
|------|--------|------|
| R² | 0.7-0.85 | 0.7-0.85 |
| 主効果選択 | X1, X2 | X1, X2 |
| 交互作用選択 | X1×X2 (二次項は検出不可) | X1×X2 |

**検証ポイント:**
- hierNetは二次項(X1², X2²)を直接モデル化しない
- 交互作用X1×X2は検出されるはず
- R²が他パターンより低いのは二次項の影響

---

### 5. `low_signal_noise` - 低信号・高ノイズモデル

**真のモデル:**
```
Y = 50 + 0.1×X1 - 0.1×X2 + 0.05×X3 + ε(σ=5)
```

**期待される結果:**
| 指標 | Strong | Weak |
|------|--------|------|
| R² | < 0.1 | < 0.1 |
| 主効果選択 | 0-1 | 0-1 |
| 交互作用選択 | 0 | 0 |

**検証ポイント:**
- **過学習を避けるためλが大きくなる**
- 変数がほぼ選択されないことが正常
- R²が非常に低い（信号がノイズに埋もれている）

---

## エッジケーステスト

### Test 1: 小サンプル (n=20)
- `nfolds`を3に減らす必要あり
- サンプルサイズ警告が出る

### Test 2: 欠損値
- 自動的に除外される
- 警告メッセージが出る

### Test 3: 定数列
- 自動的に除外される
- 警告メッセージが出る

### Test 4: 高相関変数
- 警告が出るが分析は続行
- 係数が不安定になる可能性

### Test 5: ゼロを含む目的変数
- MAPE計算でゼロ以外のみ使用
- R²は正常に計算

---

## 品質チェックリスト

### 機能テスト
- [ ] 5パターン全てで分析完了
- [ ] Strong/Weak両方で結果が異なる（特にweak_interact）
- [ ] 係数の符号が真のモデルと一致
- [ ] 交互作用ヒートマップが表示される

### エラーハンドリング
- [ ] 欠損値警告が表示される
- [ ] 定数列警告が表示される
- [ ] 小サンプル警告が表示される
- [ ] 分析完了通知にR²と選択変数数が表示される

### UI/UX
- [ ] プログレスバーが100%まで進む
- [ ] タブ切り替えが正常
- [ ] 係数テーブルがソートされている
- [ ] 回帰式が正しく表示される

---

## トラブルシューティング

### 分析が遅い
- `nlam`を減らす（10-15推奨）
- `nfolds`を減らす（3-5推奨）
- 変数数を10以下に

### R²が極端に低い
- `low_signal_noise`パターンでは正常
- データの前処理を確認
- 外れ値の確認

### 交互作用が検出されない
- Strong hierarchyの場合、主効果が先に必要
- Weak hierarchyに切り替えて試す
- λが大きすぎる可能性（nlam増加）
